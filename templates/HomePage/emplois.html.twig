{% extends 'base.html.twig' %}

{% block body %}
    <div class="dep semestres" data-aos="fade-up">
        <ul class="dep-list">
            {% for i in range(1,2) %}
            <li>
                <a data-bs-toggle="collapse" class="collapsed d-flex justify-content-center" data-bs-target="#sem{{ i }}">
                    Semestre {{ i }}
                    <i class="icon-show">
                        <iconify-icon data-icon="akar-icons:chevron-down"></iconify-icon>
                    </i>
                    <i class="icon-close">
                        <iconify-icon data-icon="akar-icons:chevron-up"></iconify-icon>
                    </i>
                </a>
                <div id="sem{{ i }}" class="collapse" data-bs-parent=".dep-list">
                    <br>
                    <div class="btn-group d-flex justify-content-center">
                        {% if i == 1  %}
                        {% set sem = sem1 %}
                        {% else %}
                            {% set sem = sem2 %}
                        {% endif %}
                        {% for emploi in sem  %}
                            {% if emploi.doc is defined and emploi.doc %}
                                <a class="btn btn-secondary"
                                   href="{{ asset(emploi.doc.getFullUrl()) }}" target="_blank">{{ emploi.filiere.filiere }}
                                </a>
                            {% endif %}
                        {% endfor %}
                    </div>
                </div>
            </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}